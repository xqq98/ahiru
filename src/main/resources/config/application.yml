spring:
  resources:
    static-locations: classpath:/static
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    url: jdbc:mysql://52.196.8.152:3306/awsdb1?serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=utf8&useSSL=false&allowMultiQueries=true
    username: usrdev
    password: Test001@
    driverClassName: com.mysql.cj.jdbc.Driver
    validationQuery: select 'x'
  freemarker:
    request-context-attribute: request  #req访问request
    suffix: .ftl
    content-type: text/html
    enabled: true
    cache: false
    template-loader-path: classpath:/templates/
    charset: UTF-8 #编码格式
    settings:
      number_format: '0.##'   #数字格式化，无小数点
  jmx:
    enabled: false
mybatis-plus:
  mapper-locations: classpath:/config/mapper/**/*Mapper.xml
server:
  servlet:
    context-path: /ahiru

mybatis:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

logging:
  level:
    com:
      webservice:
        ahiru:
          mapper: debug